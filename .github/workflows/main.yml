name: Hello world
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build step
        run: echo "This is my {{ vars.MY_NAME0 }}"
      - name: Docker secrets
        run: echo "Docker username is ${{ secrets.DOCKER_USERNAME }}"
  
      
