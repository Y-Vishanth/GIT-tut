name: Hello world
on: workflow_dispatch
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Hello world
        run: |
          echo "Hello world!"
          echo "This is a GitHub Action workflow example."
